<form method="post" action="{{ path('donation_bag_delete', {'id': donation_bag.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ donation_bag.id) }}">
    <button class="btn">Delete</button>
</form>