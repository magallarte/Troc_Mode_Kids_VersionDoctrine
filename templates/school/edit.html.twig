{% extends 'base.html.twig' %}

{% block title %}Modification d'une école{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier les données d'une école</h1>
        <form class="form-group" method="post">
        {{ include('school/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </form>
        <a href="{{ path('school_index') }}">Retour à la liste</a>
        <br>
        {{ include('school/_delete_form.html.twig') }}
    </div>
{% endblock %}