{% extends 'base.html.twig' %}

{% block title %}Fabric index{% endblock %}

{% block body %}
    <h1>Fabric index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fabric_name</th>
                <th>Fabric_code</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fabric in fabrics %}
            <tr>
                <td>{{ fabric.id }}</td>
                <td>{{ fabric.fabric_name }}</td>
                <td>{{ fabric.fabric_code }}</td>
                <td>
                    <a href="{{ path('fabric_show', {'id': fabric.id}) }}">show</a>
                    <a href="{{ path('fabric_edit', {'id': fabric.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('fabric_new') }}">Create new</a>
{% endblock %}