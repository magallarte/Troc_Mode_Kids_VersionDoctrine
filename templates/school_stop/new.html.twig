{% extends 'base.html.twig' %}

{% block title %}Créer une tournée{% endblock %}

{% block body %}
    <div class="container">
        <h1>Créer une tournée</h1>
        <form class="form-group" method="post">
        {{ include('school_stop/_form.html.twig', {'button_label': 'Créer'}) }}
        </form>
        <a href="{{ path('school_stop_index') }}">Retour à la liste</a>
        <br>
        {{ include('school_stop/_delete_form.html.twig') }}
    </div>
{% endblock %}