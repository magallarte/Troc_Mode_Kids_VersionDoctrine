{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Fiche Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Catégorie :</th>
                <td>{{ article.articleType }}</td>
            </tr>
            <tr>
                <th>Sexe:</th>
                <td>{{ article.articleGender }}</td>
            </tr>
            <tr>
                <th>Saison</th>
                <td>{{ article.articleSeason }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ article.articleSize }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ article.articleBrand }}</td>
            </tr>
            <tr>
                <th>Photo 1</th>
                <td>{{ article.articlePicture1 }}</td>
            </tr>
            <tr>
                <th>Photo 2</th>
                <td>{{ article.articlePicture2 }}</td>
            </tr>
            <tr>
                <th>Photo 3</th>
                <td>{{ article.articlePicture3 }}</td>
            </tr>
            <tr>
                <th>Etat d'usure</th>
                <td>{{ article.wearStatus }}</td>
            </tr>
            <tr>
                <th>Valeur (boutons)</th>
                <td>{{ article.articleButtonValue }}</td>
            </tr>
            <tr>
                <th>Valeur (euros)</th>
                <td>{{ article.articleEurosValue }}</td>
            </tr>
            <tr>
                <th>Descriptif</th>
                <td>{{ article.articleComments }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('article_index') }}">back to list</a>

    <a href="{{ path('article_edit', {'id': article.id}) }}">edit</a>

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}

